@{
    ViewData["Title"] = "Search for a Joke";
}

<h4>Search for a Joke</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="get" action="@Url.Action("ShowSearchResults")">
            <div class="form-group">
                <label for="SearchPhrase" class="control-label">Search Phrase</label>
                <input name="searchPhrase" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Search" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
